description "xdebug-netbeans-bridge"
author "--- <a@b.com>"
 
# When to start the service
start on runlevel [2345]
 
# When to stop the service
stop on runlevel [016]
 
# Automatically restart process if crashed
respawn
 
# Essentially lets upstart know the process will detach itself to the background
# expect fork
 
# Run before process
pre-start script

end script
 
# Start the process
script
su -c "/usr/bin/php -S localhost:9004 /usr/bin/local/xdebug-netbeans-bridge/xdebug-netbeans-bridge.php"
end script